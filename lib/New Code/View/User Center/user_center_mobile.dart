import 'package:flutter/material.dart';

class UserCenterMobile extends StatelessWidget {
  const UserCenterMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("User Center"),
    );
  }
}
